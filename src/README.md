# zpass

zpass is CLI-based password manager.

## Usage

Just call command `zpass` on your terminal.

```sh
$ ./target/release/zpass -h
? Pick your operation ›
❯ search
  create
  update
  delete
```
